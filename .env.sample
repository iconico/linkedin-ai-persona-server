# --- Server Configuration ---
# The GCS bucket where your fine-tuned models are stored.
GCS_BUCKET="your-gcs-bucket-name-here"

# The name of the specific model folder within the 'tuned-models' directory in your GCS bucket.
MODEL_NAME="your-model-name-here"